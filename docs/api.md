# API Reference

This project is a static browser app. It does not expose custom HTTP routes or a
server-side API. Its integration contract is made of:

- the optional Supabase browser API used for cloud sync;
- the `rehab_sessions` database table;
- the browser `localStorage` keys used for offline state;
- the DOM IDs and events that `script.js` expects.

## Runtime Configuration

Cloud sync is configured in `index.html` before `script.js` loads.

```html
<script>
  window.KNEE_REHAB_SUPABASE_CONFIG = {
    url: "https://YOUR_PROJECT_REF.supabase.co",
    anonKey: "YOUR_SUPABASE_ANON_KEY"
  };
</script>
```

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `url` | string | Yes, for sync | Supabase project URL. Placeholder values disable sync. |
| `anonKey` | string | Yes, for sync | Supabase anon public key. Placeholder values disable sync. |

If the config is missing or still contains placeholders, the app remains fully
local and shows the sync panel as disabled.

## Supabase Auth API

The app uses Supabase Auth through the browser client loaded from
`@supabase/supabase-js@2`.

| UI action | Supabase method | Required inputs |
| --- | --- | --- |
| Sign in | `auth.signInWithPassword(credentials)` | `email`, `password` |
| Create account | `auth.signUp(credentials)` | `email`, `password` |
| Sign out | `auth.signOut()` | Active auth session |
| Restore session | `auth.getSession()` | Existing browser auth session |
| React to auth changes | `auth.onAuthStateChange(callback)` | Supabase client |

Sync operations only run when a Supabase client exists and a user is signed in.

## Supabase Data API

All cloud data is stored in the `public.rehab_sessions` table.

| Operation | Supabase query |
| --- | --- |
| Load sessions | `from("rehab_sessions").select("*").eq("user_id", user.id).order("date_key", { ascending: true })` |
| Save one session | `from("rehab_sessions").upsert(row, { onConflict: "user_id,date_key" })` |
| Seed local sessions after first sign-in | `from("rehab_sessions").upsert(rows, { onConflict: "user_id,date_key" })` |

The row-level security policies in `supabase-schema.sql` restrict select,
insert, update, and delete operations to rows where `auth.uid() = user_id`.

## Database Schema

Source: `supabase-schema.sql`

| Column | Type | Required | Description |
| --- | --- | --- | --- |
| `id` | `uuid` | Yes | Primary key generated by `gen_random_uuid()`. |
| `user_id` | `uuid` | Yes | References `auth.users(id)` and cascades on user deletion. |
| `date_key` | `date` | Yes | Session date in `YYYY-MM-DD` form. |
| `completed` | `boolean` | Yes | Whether the date is marked complete. |
| `manual_completion` | `boolean` | Yes | Whether completion was manually toggled. |
| `checklist` | `jsonb` | Yes | Snapshot of checklist item states. |
| `set_rows` | `jsonb` | Yes | Snapshot of set timer rows. |
| `timer` | `jsonb` | Yes | Snapshot of the session timer. |
| `notes` | `text` | Yes | Session notes. |
| `exercises` | `jsonb` | Yes | Per-exercise progress summary. |
| `monitoring` | `jsonb` | Yes | Post-rehab monitoring checks. |
| `updated_at` | `timestamptz` | Yes | Last saved timestamp. |

Unique constraint:

```sql
unique (user_id, date_key)
```

Index:

```sql
rehab_sessions_user_date_idx on public.rehab_sessions (user_id, date_key)
```

## Session Row Shape

`script.js` creates rows with this shape before calling Supabase `upsert`.

```json
{
  "user_id": "00000000-0000-0000-0000-000000000000",
  "date_key": "2026-06-12",
  "completed": false,
  "manual_completion": false,
  "checklist": {
    "wall-setup": true,
    "wall-angle": false
  },
  "set_rows": {
    "wall-1": {
      "elapsedMs": 0,
      "startedAt": null,
      "isRunning": false,
      "isDone": false,
      "isRepLoop": true,
      "totalReps": 10,
      "currentRep": 1,
      "repState": "work",
      "timeRemainingSec": 30,
      "workDurationSec": 30,
      "restDurationSec": 15
    }
  },
  "timer": {
    "elapsedMs": 0,
    "startedAt": null,
    "isRunning": false
  },
  "notes": "",
  "exercises": {
    "wall": {
      "label": "Wall Sit",
      "status": "Not started",
      "statusKey": "status.notStarted",
      "completedSets": 0,
      "totalSets": 2,
      "completedChecks": 0,
      "totalChecks": 5
    }
  },
  "monitoring": {
    "immediate": false,
    "nextMorning": false,
    "walking": false
  },
  "updated_at": "2026-06-12T00:00:00.000Z"
}
```

## Browser Storage API

The app stores local state in `localStorage`. These keys are part of the
client-side persistence contract.

| Key | Value format | Description |
| --- | --- | --- |
| `kneeRehabChecklistState` | JSON object | Checkbox ID to boolean state. |
| `kneeRehabNotes` | string | Current session notes. |
| `kneeRehabLastUpdated` | ISO datetime string | Last local update timestamp. |
| `kneeRehabTimerState` | JSON object | Session timer snapshot. |
| `kneeRehabSetRowState` | JSON object | Set row timer and completion snapshots. |
| `kneeRehabCompletedDates` | JSON array | Completed date keys in `YYYY-MM-DD` form. |
| `kneeRehabSessionHistory` | JSON object | Date-keyed session history records. |
| `kneeRehabVoiceCuesEnabled` | string boolean | `"true"` or `"false"`. |
| `kneeRehabChecklistCollapseState` | JSON object | Checklist section ID to collapsed boolean. |
| `kneeRehabLanguage` | string | Supported language code: `en` or `th`. |

Invalid JSON in structured keys is handled defensively: the app removes or
replaces corrupted values and continues with defaults.

## Checklist IDs

Checklist state is keyed by stable checkbox IDs.

| Group | IDs |
| --- | --- |
| Wall Sit | `wall-setup`, `wall-angle`, `wall-foot-position`, `wall-data-check`, `wall-safety-check` |
| Straight Leg Raise | `slr-setup`, `slr-lock-twist`, `slr-tempo-lift`, `slr-tempo-hold`, `slr-tempo-lower`, `slr-data-check` |
| Glute Bridges | `bridge-setup`, `bridge-core`, `bridge-lift`, `bridge-safety-check` |
| Clamshells | `clam-setup`, `clam-hip-stack`, `clam-control`, `clam-safety-check` |
| Monitoring | `monitor-immediate`, `monitor-next-morning`, `monitor-walking` |

## Set Row IDs

Set row state is keyed by stable set IDs.

| Exercise | Set IDs | Work/rest timing |
| --- | --- | --- |
| Wall Sit | `wall-1`, `wall-2` | 30 seconds work, 15 seconds rest, 10 reps |
| Straight Leg Raise | `slr-1`, `slr-2`, `slr-3` | 5 seconds work, 3 seconds rest, 15 reps |
| Glute Bridges | `bridge-1`, `bridge-2` | 5 seconds work, 3 seconds rest, 15 reps |
| Clamshells | `clam-1`, `clam-2` | 5 seconds work, 3 seconds rest, 15 reps |

## DOM Integration Contract

`script.js` binds event listeners during `DOMContentLoaded`. The following IDs
must stay stable unless the script is updated at the same time.

| Area | Required IDs |
| --- | --- |
| Supabase auth | `sync-auth-form`, `sync-email`, `sync-password`, `sync-sign-in`, `sync-sign-up`, `sync-sign-out`, `sync-status`, `sync-user` |
| Session summary | `session-date`, `last-updated`, `session-timer`, `timer-toggle`, `timer-reset` |
| Voice cues | `voice-cues-toggle`, `voice-cues-status` |
| Progress | `completed-count`, `total-count`, `progress-fill` |
| Calendar | `calendar-prev`, `calendar-next`, `calendar-month`, `calendar-grid`, `mark-today-complete`, `selected-calendar-date`, `toggle-selected-complete`, `session-history-panel` |
| Notes and reset | `session-notes`, `clear-notes`, `reset-checklist`, `reset-all-sets` |

Checklist toggles also depend on `.checklist-toggle[aria-controls]`, and set row
controls depend on generated IDs based on the set row IDs above.

## Completion Rules

The "Mark Today Complete" button becomes available only when:

- the selected date is today;
- at least one exercise is fully finished;
- the immediate post-rehab monitoring check is complete;
- today is not already marked complete.

Future dates can be selected for viewing, but editing controls are disabled.

## Error Handling

- Missing Supabase config disables sync and preserves local-only behavior.
- Supabase load/save/auth failures are logged with `console.warn` and displayed
  through the sync status element.
- Running timers are paused when session snapshots are written to history or
  restored from saved records.
